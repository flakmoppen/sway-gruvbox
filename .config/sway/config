# Read `man 5 sway` for a complete reference.
#exec --no-startup-id systemctl --user import-environment SWAYSOCK
#exec --no-startup-id dbus-update-activation-environment --systemd SWAYSOCK
exec_always brillo -S $(cat $HOME/.config/sway/brightness/value)

# Variables
set {
    # Use Alt as modifier.
    $mod Mod1

    # Directional keys
    $left   j
    $down   k
    $up     i
    $right  l

    # Apps
    $menu       wofi --show
    $term       alacritty
    $browser    floorp
    $editor     placeholder
}


# Wallpaper
output * bg /home/eddo/Documents/Wallpapers/lotus-wallpaper.png fill


# Idle configuration
# To reload swayidle use: killall swayidle
# and set 'exec' to 'exec_always' below.
# To avoid having  multiple swayidle sessions running in the background,
# don't forget to reset to 'exec' after testing is done.
exec swayidle -w \
    timeout 30 "brillo -O && brillo -G > $HOME/.config/sway/brightness/value && brillo -u 1000000 -q -S 15" \
	resume "brillo -S $(cat $HOME/.config/sway/brightness/value)" \
    timeout 120 "swaymsg 'output * dpms off'" \
	resume "swaymsg 'output * dpms on'"


### Input configuration
# You can get the names of your inputs by running: swaymsg -t get_inputs
# If you get a "Unable to retrieve socket path" error. Run:
# export SWAYSOCK=/run/user/$(id -u)/sway-ipc.$(id -u).$(pgrep -x sway).sock
# Read `man 5 sway-input` for more information about this section.
input "1:1:AT_Translated_Set_2_keyboard" {
	xkb_layout "se"
	xkb_numlock enabled
}


# Key to use to manipulate floating windows.
# Change 'normal' to 'inverse' to switch right and left mouse button.
floating_modifier $mod normal

# Including bindings and window management:
include {
    ~/.config/sway/config.d/*
}

# Status Bar:
bar {
	swaybar_command waybar
}
